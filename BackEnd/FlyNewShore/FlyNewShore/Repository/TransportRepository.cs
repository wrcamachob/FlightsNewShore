using FlyNewShore.Entities;

namespace FlyNewShore.Repository
{
    public class TransportRepository
    {
        public async Task<Transport> GetTransport(string flightNumb) 
        {
            var transport = new Transport();

            return transport;
        }
    }
}
